import React from "react";
import { ArrowRightShortIcon, MailIcon } from "../../../utils/icons";
import Button from "./Button";

const MailBtn = ({ btn }) => {
  return <Button {...{ btn }} />;
};

export default MailBtn;
