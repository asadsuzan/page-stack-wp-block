import React from 'react'

const PageDescription = ({ description }) => {
    return (
        <p className="ps_description">
            {description}
        </p>
    )
}

export default PageDescription